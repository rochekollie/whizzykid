/* jshint esversion: 8 */

import { dynamicRoute, generateStaticPage } from './loader.mjs';

(function () {

	"use strict";

	// const pageLinks = document.querySelectorAll("li");

	// pageLinks.forEach((link) => {
	// 	link.addEventListener("click", () => {
	// 		dynamicRoute(link.innerHTML);
	// 	})
	// });

	// generateStaticPage();
	console.log(window.location.href);


}());
